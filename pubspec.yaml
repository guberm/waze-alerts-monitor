name: waze_alerts
description: Real-time Waze alert monitoring with voice notifications and GPS tracking.
publish_to: 'none'

version: 1.0.2+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.0.0
  flutter_tts: ^4.2.0
  http: ^1.1.0
  permission_handler: ^11.0.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.0
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
